const express=require('express'),cors=require('cors'),helmet=require('helmet'),morgan=require('morgan'),rate=require('./middlewares/rateLimit.middleware');const {clientOrigin}=require('./config/config');const requestId=require('./middlewares/requestId.middleware');const auth=require('./middlewares/auth.middleware');
const app=express();app.use(helmet({contentSecurityPolicy:false}));app.use(cors({origin:clientOrigin==='*'?true:clientOrigin}));app.use(express.json({limit:'100kb'}));app.use(requestId);app.use(morgan('tiny'));app.use(rate);
app.get('/health',(req,res)=>res.json({status:'ok',service:'veridian-internal-service-agent',time:new Date().toISOString()}));app.use('/api/v1/auth',require('./routes/auth.routes'));app.use('/api/v1/requests',auth,require('./routes/request.routes'));app.use('/api/v1/tickets',auth,require('./routes/ticket.routes'));app.use('/api/v1/agent',auth,require('./routes/agent.routes'));app.use('/api/v1/kb',auth,require('./routes/kb.routes'));app.use('/api/v1/dashboard',auth,require('./routes/dashboard.routes'));app.use('/api/v1/audit',auth,require('./routes/audit.routes'));app.use((req,res)=>res.status(404).json({success:false,error:{code:'NOT_FOUND',message:'Route not found',details:[]}}));app.use(require('./middlewares/error.middleware'));module.exports=app;
